<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 对于Android 10及以上版本，如果需要访问所有文件，可以添加 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- 如果需要访问相机和存储的特殊权限 -->
    <application
        android:allowBackup="true"
        android:label="@string/app_name">

        <activity android:name=".activity.DynamicDetailsActivity"
            android:exported="false" />
        <activity android:name=".friend.AddFriendActivity"
            android:exported="false" />
        <activity android:name=".friend.FriendMessageActivity"
            android:exported="false" />
        <activity android:name=".chat.ChatActivity"
            android:exported="false" />
        <activity android:name=".activity.DynamicMassageActivity"
            android:exported="false" />
    </application>
</manifest>